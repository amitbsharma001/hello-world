# ── Core Django ───────────────────────────────────────────────────────────────
DJANGO_SECRET_KEY=change-me-to-a-50-char-random-string
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=app.yourcompany.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://app.yourcompany.com
# DJANGO_SSL_REDIRECT=1            # set 0 only if TLS terminates elsewhere

# ── Database (Postgres) ──────────────────────────────────────────────────────
POSTGRES_DB=approvals
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ── Celery / Redis ───────────────────────────────────────────────────────────
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1
CELERY_EAGER=0                     # MUST be 0 in production

# ── Email (set EMAIL_HOST and SMTP turns on automatically) ───────────────────
EMAIL_HOST=smtp.yourprovider.com
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=change-me
EMAIL_USE_TLS=1
DEFAULT_FROM_EMAIL=Approvals <noreply@yourcompany.com>

# ── Notifications ────────────────────────────────────────────────────────────
NOTIFICATION_CHANNELS=inapp,email  # drop "email" to silence all mail
